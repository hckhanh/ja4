# Setup

Always run `vp install` before doing any other work in this project. This installs dependencies and runs the `prepare` script which calls `vp config && skills-npm` to discover and symlink agent skills from npm packages.

Do this first, before running tests, linting, building, or making any code changes.
